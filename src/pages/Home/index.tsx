import React from 'react';
import { ButtonComponent, Screen, EvaluationComponent } from '../../components';

const Home: React.FC = () => (
  <Screen>
   <ButtonComponent/>
  </Screen>
);

export default Home;
