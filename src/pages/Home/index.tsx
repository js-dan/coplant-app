import React from 'react';
import { ButtonComponent, Screen, PlantComponent, AddPlantComponent, PresentCardComponent, InsertImageComponent} from '../../components';

const Home: React.FC = () => (
  <Screen>
   <InsertImageComponent/>
  </Screen>
);

export default Home;
