import React from 'react';
import { ButtonComponent, Screen } from '../../components';

const Home: React.FC = () => (
  <Screen>
    <ButtonComponent
      buttonColor="orange"
      buttonText="eitaPlantinha"
      size="small"
    />
  </Screen>
);

export default Home;
